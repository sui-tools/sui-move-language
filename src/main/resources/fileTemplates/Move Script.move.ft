script {
    // Import statements
    use std::signer;
    
    public entry fun ${NAME}(account: signer) {
        // Script logic here
        let addr = signer::address_of(&account);
        // TODO: Implement script logic
    }
}
